# PostgreSQL connection — update these before running
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/rfid_race_db

# Internal Army verification server — leave blank to use mock verification
VERIFICATION_SERVER_URL=

# App
APP_ENV=development
DEBUG=True
